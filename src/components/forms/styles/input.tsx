const labelStyles = 'mb-1 text-base'
const disabledStyles = 'cursor-not-allowed opacity-50'
const errorStyles = 'border-red'
const basicStyles = 'w-full pl-4 relative'
const requiredStyles = "after:text-red-600 after:content-['*'] after:ml-1"
const inputStyles = 'h-10 bg-transparent border rounded'
const textareaStyles = 'pt-2 bg-transparent border rounded resize-none min-h-28'
const noErrorsStyles = 'border-border'
const errorWarningStyles = 'text-red-600 text-sm mt-2'
const uploadStyles = 'min-h-28 bg-transparent border rounded'

export {
  labelStyles,
  disabledStyles,
  errorStyles,
  basicStyles,
  requiredStyles,
  inputStyles,
  textareaStyles,
  noErrorsStyles,
  errorWarningStyles,
  uploadStyles,
}
